/**
 * JS controller for the 4th newtab prototype
 */

/* create a new slider */
$(function(){
  var header = new Header();
  var slider = new Slider();
  var footer = new Footer();

  /* create a Panel for the first panel */
  var first_panel = new Panel($("#panel_0"));

})